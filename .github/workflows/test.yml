name: SimpleExample

on: [push]

jobs:
  example:
    runs-on: ubuntu-latest
    steps:
    # Checkout
    - uses: actions/checkout@v3
    # Build
    - run: echo 'it works fine'
# This is a simple workflow for a python app